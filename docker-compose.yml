services:
  dev:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    image: smart-geyser-dev
    working_dir: /workspace
    volumes:
      - .:/workspace
      - cargo-registry:/usr/local/cargo/registry
      - cargo-git:/usr/local/cargo/git
      - target-cache:/workspace/target
    environment:
      CARGO_TERM_COLOR: always
    command: bash

volumes:
  cargo-registry:
  cargo-git:
  target-cache:
